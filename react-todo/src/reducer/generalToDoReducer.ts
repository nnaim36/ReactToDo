import {createSlice } from "@reduxjs/toolkit";

const initialState = {
    todos:[],
}

const genTodoSlice = createSlice({
    name:"genTodo",
    initialState,
    reducers:{

    },
    deleteTask: ()=>{

    },
})